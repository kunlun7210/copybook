# Safari 打印回归 2026-09-08

- 移除顶部整条导航。存档入口保留在保存对话框内。
- 将打印从缩放 iframe 移到独立页面，页面正常文档流不依赖编辑页的滚动和缩放。
- 英文字体使用可被打印文档识别的 CSS @font-face，生成前等待字体就绪。打印调用安排在点击处理完成后执行，避免 Safari 保存 PDF 时无响应。
- 打印纸张高度留 0.2mm 的舍入余量，解决 Safari A4 实际打印多出空白页；四线格和文字不缩放。

## 实际验证

macOS 15.7.9 Safari 系统打印面板，A4，100%，保持浏览器页眉页脚选项开启。未发送实体打印任务。

1. 默认四词：系统打印 1 页，实际保存的 Quartz PDF 为 A4、1 页、32468 bytes。渲染检查首行 rule、中文、音标、例句、四线格均完整。
2. 24 行词表：apple/book/hand/playground 循环，中文编号 1..24。网页 4 页，滚动到后面再系统打印仍为4页。实际 PDF 4 页、33892 bytes，各页分界 1..7、8..14、15..21、22..24。所有页面渲染检查通过，所有英文内容在纸内。英文上边界均约47.95pt；前三页最下边界约711.82pt，纸高841.89pt。
3. 单页与多页保存完成后 Safari 页面保持可响应。早期失败的空白 PDF 未当作通过证据。

GitHub Pages 使用同一静态构建，资源改为相对路径。实体打印机出纸和真实移动设备系统打印尚未测试。

打印页面按钮在视口水平居中；窄屏页数提示移至下一行。打印媒体中整个工具条仍隐藏。
